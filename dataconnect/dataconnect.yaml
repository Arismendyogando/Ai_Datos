specVersion: "v1beta"
serviceId: "analizer"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "ANLIZER-SQL"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
